(function (window) {
  window.__env = window.__env || {};
  const env = window.__env;
  env.feedbackUrl =
    "";
  env.production = false;
  env.serverAPI = "";
  env.serverPortal = "";
  env.wskey = "";
})(this);
