apiVersion: v1
kind: Service
metadata:
  labels:
    app: statistics-dashboard-ui-service-test
  name: statistics-dashboard-ui-service-test
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: statistics-dashboard-ui-test
  type: ClusterIP
